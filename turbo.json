{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["packages/@real-token/src/**"],
      "outputs": ["dist/**"]
    },
    "yalc:push": {
      "dependsOn": ["^build"]
    },
    "yalc:add": {},
    "clean-install": {}
  }
}
